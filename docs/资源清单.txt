1. 后端控制台服务器：2台 4c8g：也可以一台，但是一台的话上线期间管理系统会不可用
2. 数据库服务器：一主2从，用来存ducc配置以及 接口配置
3. 发布服务器：2台 8c16g：最低2台，发布的服务需要不间断提供服务,将来根据访问量需要能够动态扩容
4. ducc：2台机器

域名：2个，控制台服务一个，发布服务器一个


1. webservice接口转换为http接口，并调用
2. http出参、入参都是json，对入参、出参重新映射，数据转换包括日期转换、数据转换
3. http入参是xml，加工后发起请求
4. 并行步骤、判断步骤演示
5. http步骤转换为webservice步骤


监控体系实现方案：
1. 存放到es里面，直接查询
2. 数据量更大的话，mq+flink计算，结果再存放到es或者hbase里